meta:
  environment: docker
  toolchain:
    name: docker

build:
  dockerfile: Dockerfile   # если Dockerfile лежит внутри rental/

run:
  containerPort: 80        # приложение слушает порт 80 (как в Dockerfile)
  # Если нужно постоянное хранилище для файлов (не для БД) – раскомментируйте:
  # persistenceMount: /app/data